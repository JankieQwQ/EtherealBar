class Bar {
    constructor(version) {
        this.version = version;
    }

    bartending() {
        const music = new Audio('adf.wav');
        music.loop = true;
        music.playbackRate = 2;
        music.play();
    }
}
